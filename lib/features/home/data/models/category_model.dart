class CategoryModel {
  final String title;
  final String iconPath;
  final int id; // Changed to int

  const CategoryModel({
    required this.title,
    required this.iconPath,
    required this.id,
  });
}
